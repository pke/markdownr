Your markdown files can now use common HTML inline tags — and they'll actually render properly. Bold, italic, strikethrough, underline, highlight, superscripts, subscripts, line breaks — all the classics. Tags inside code spans stay as literal text, because that's just good manners.